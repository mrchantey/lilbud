


AFRAME.registerSystem('colorGenerator', {
})
        // getRandomDelta3D(deltaTime, maxSpeed) {
        //     return new THREE.Vector3(
        //         this.getRandomDelta1D(deltaTime, maxSpeed),
        //         this.getRandomDelta1D(deltaTime, maxSpeed),
        //         this.getRandomDelta1D(deltaTime, maxSpeed)
        //     )
        // }
        // , getRandomDelta1D(deltaTime, maxSpeed) {
        //     return (Math.random() * 2 - 1) //-range -1,1
        //         * this.data.maxSpeed // range -maxSpeed,maxSpeed
        //         * deltaTime // scale to meters per second
        // }